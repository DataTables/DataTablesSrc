<?xml version="1.0" encoding="UTF-8" ?>
<dt-option group="i18n">
	<name>language.paginate.first</name>
	<summary>Pagination 'first' button string</summary>
	<since>1.10</since>

	<type type="string" />
	<default value="«" />

	<description>
		<![CDATA[
		
		Text used when the pagination control shows the button to take the user to the first page.

		Note that DataTables writes this property to the document as HTML, so you can use HTML tags in the language string, but HTML entities such as `<` and `>` should be encoded as `&lt;` and `&gt;` respectively.

    	Please note that the default value of this parameter was changed in DataTables 2. It is now `«`, while in DataTables 1.x it was `First`.

		]]>
	</description>

	<example title="Set text for pagination 'first' button"><![CDATA[
new DataTable('#myTable', {
	language: {
		paginate: {
			first: 'First page'
		}
	}
});
]]></example>

	<related type="option">language</related>
	<related type="option">language.paginate</related>
	<related type="option">language.paginate.last</related>
	<related type="option">language.paginate.next</related>
	<related type="option">language.paginate.previous</related>
	<related type="option">language.aria.paginate</related>
	<related type="option">language.aria.paginate.first</related>
</dt-option>
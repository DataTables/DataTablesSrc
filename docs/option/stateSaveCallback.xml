<?xml version="1.0" encoding="UTF-8" ?>
<dt-option group="callback">
	<name>stateSaveCallback</name>
	<summary>Callback that defines how the table state is stored and where.</summary>
	<since>1.10</since>

	<type type="function">
		<signature>stateSaveCallback( settings, data )</signature>
		<parameter type="DataTable.Context" name="settings">
			DataTables settings object
		</parameter>
		<parameter type="DataTable.State" name="data">
			Data to save. The data comes from `-init stateSaveParams`
		</parameter>
		<scope>HTML table element</scope>
	</type>

	<description>
		DataTables saves the state of the table (paging, filtering etc) and by default it will use HTML5's `localStorage` to save the state into. When the `-init stateSave` option is enabled, this callback method allows you to change where the state is saved (for example you might wish to use a server-side database or cookies).

		The data given to the function has the structure defined by `-type DataTable.State`. Note that additional extensions can add extra information to this structure, or you may use `-init stateSaveParams` or `-event stateSaveParams` to add your own parameters. Also the information stored is type sensitive - that is, the data type of the data given by DataTables must be preserved. For example the `start` parameter must be a `-type number` data type.

		This method is required only to store the data given to it. The `-init stateSaveParams` method is used to manipulate the data that is to actually be saved.

		This callback works hand-in-hand with `-init stateLoadCallback`. This method saves the state while `-init stateSaveCallback` will load it from where this callback has saved it.
	</description>

	<example title="Save state on a server with Ajax"><![CDATA[
new DataTable('#myTable', {
	stateSave: true,
	stateSaveCallback: function (settings, data) {
		// Send an Ajax request to the server with the state object
		DataTable.ajax({
			url: '/state_save',
			data: data,
			dataType: 'json',
			type: 'POST'
		});
	}
});
]]></example>

	<related>-api state()</related>
	<related>-api state.clear()</related>
	<related>-api state.loaded()</related>
	<related>-api state.save()</related>
	<related>-init stateSave</related>
	<related>-init stateLoadParams</related>
	<related>-init stateLoaded</related>
	<related>-init stateLoadCallback</related>
	<related>-init stateSaveParams</related>
	<related>-event stateLoaded</related>
	<related>-event stateLoadParams</related>
	<related>-event stateSaveParams</related>
</dt-option>

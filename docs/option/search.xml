<dt-option group="option">
	<name>search</name>
	<summary>Set an initial search in DataTables and / or search options.</summary>
	<since>1.10</since>

	<type type="object" />

	<description>
		The `-init search` option allows the way DataTables performs filtering to be set during the initialisation, and to set an initial global filter.

		The options that control how the search operates are defined by the `-type DataTables.SearchOptions` object type. You can pass in an object with any or all of the options described by that parameter.

		Additionally, to set an initial search term use the parameter `search` in this object.
	</description>

	<example title="Set an initial filter"><![CDATA[
new DataTable('#myTable', {
	search: {
		search: 'Fred'
	}
});
]]></example>

	<example title="Set search to use boundary conditions"><![CDATA[
new DataTable('#myTable', {
	search: {
		boundary: true
	}
});
]]></example>

	<related>-init search</related>
	<related>-init searchCols</related>
	<related>-api search()</related>
	<related>-api columns().search()</related>
</dt-option>
<?xml version="1.0" encoding="UTF-8" ?>
<dt-api group="core">
	<name>state.loaded()</name>
	<summary>Get the table state that was loaded during initialisation.</summary>
	<since>1.10.1</since>

	<type type="function">
		<signature>state.loaded()</signature>
		<description>Get the table state that was loaded during initialisation</description>
		<returns type="object">State saved object. See `-api state()` for the object format.</returns>
	</type>

	<description>
		This method is primarily made available for plug-in authors who wish to get the state of a DataTable when it is initialised to restore state saved functionality of their own plug-in.

		The structure of the object returned by this method matches the state object, as defined by `-api state()`.
	</description>

	<example title="Listen for DataTables initialisation and get the state"><![CDATA[
document.addEventListener('init', function (e, settings) {
	let api = new DataTable.Api(settings);
	let state = api.state.loaded();

	// ... use `state` to restore information
});
]]></example>

	<related>-api state()</related>
	<related>-api state.clear()</related>
	<related>-api state.save()</related>
	<related>-init stateSave</related>
	<related>-init stateSaveCallback</related>
	<related>-init stateLoadCallback</related>
	<related>-init stateLoadParams</related>
	<related>-init stateSaveParams</related>
	<related>-init stateLoaded</related>
	<related>-event stateLoaded</related>
	<related>-event stateLoadParams</related>
	<related>-event stateSaveParams</related>
</dt-api>
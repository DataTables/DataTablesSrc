describe( "jQueryUI option- DEPRECIATED", function() {
	// dt.libs( {
	// 	js:  [ 'jquery', 'datatables' ],
	// 	css: [ 'datatables' ]
	// } );

  // dt.html( 'basic' );
  // it("Header elements are fully wrapped by DIVs", function () {
  //   $('#example').dataTable({
  //     "jQueryUI": true
  //   });
  //   expect($('#example thead th').childNodes === 0).toBeTruthy();
  // });
  //
  // it("One div for each header element", function () {
  //   expect($('#example thead th div').length == 5).toBeTruthy();
  // });
  // it("One span for each header element, nested as child of div", function () {
  //   expect($('#example thead th div>span').length == 5).toBeTruthy();
  // });
} );

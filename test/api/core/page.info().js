// todo tests
// 1- Examine the returned object and make sure the properties match with what we want
// 2- Test with paging enabled and disabled

describe( "core- page.info()", function() {
	dt.libs( {
		js:  [ 'jquery', 'datatables' ],
		css: [ 'datatables' ]
	} );

	describe("Check the defaults", function () {
		dt.html( 'basic' );
		it("Default should be null", function () {
				});

	});

});

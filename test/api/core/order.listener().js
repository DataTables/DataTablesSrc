// todo tests
// 1- Expects 3 paremeters, none optional
// 2- Check each paremeter to make sure it does what we want
// 3- draws automatically
// 4- test some different types of sort using the event listeners etc


describe( "core- order.listener()", function() {
	dt.libs( {
		js:  [ 'jquery', 'datatables' ],
		css: [ 'datatables' ]
	} );

	describe("Check the defaults", function () {
		dt.html( 'basic' );
		it("Default should be null", function () {
				});

	});

});

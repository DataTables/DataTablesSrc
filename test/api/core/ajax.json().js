// todo tests
//1
// Test that ajax.json() enables access to the returned data.
// Not sure of other tests we can do here
describe( "core- ajax.json()", function() {
	dt.libs( {
		js:  [ 'jquery', 'datatables' ],
		css: [ 'datatables' ]
	} );

	describe("Check the defaults", function () {
		dt.html( 'basic' );
		it("Default should be null", function () {
				});

	});

});

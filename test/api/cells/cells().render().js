// todo tests
// 1- Only one parameter of type string
// 2- Test the 4 datatypes to get
// 3- dont quite understand how this works.
describe( "cells- cells().render()", function() {
	dt.libs( {
		js:  [ 'jquery', 'datatables' ],
		css: [ 'datatables' ]
	} );

	describe("Check the defaults", function () {
		dt.html( 'basic' );
		it("Default should be null", function () {
				});

	});

});

// todo tests
// TK COLIN
// function
// api
// returns integer
// count number of 
// rows
// columns
// cell
// table.data().count()
// And nodes cells.nodes, columns.nodes, etc

// check code for more

describe( "count()", function() {
	dt.libs( {
		js:  [ 'jquery', 'datatables' ],
		css: [ 'datatables' ]
	} );

	describe("Check the defaults", function () {
		dt.html( 'basic' );
		it("Default should be null", function () {
				});

	});

});

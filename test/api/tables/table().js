// todo tests
// 1- Selector returns correct table- add 2 tables, one with class select table using class selector in method.

describe( "tables- table()", function() {
	dt.libs( {
		js:  [ 'jquery', 'datatables' ],
		css: [ 'datatables' ]
	} );

	describe("Check the defaults", function () {
		dt.html( 'basic' );
		it("Default should be null", function () {
				});

	});

});

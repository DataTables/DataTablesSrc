// todo tests
// 1- Make sure node returned is <tfoot> for the footer. Add class and then check DOM to see if correct node was selected.

describe( "tables- table().footer()", function() {
	dt.libs( {
		js:  [ 'jquery', 'datatables' ],
		css: [ 'datatables' ]
	} );

	describe("Check the defaults", function () {
		dt.html( 'basic' );
		it("Default should be null", function () {
				});

	});

});

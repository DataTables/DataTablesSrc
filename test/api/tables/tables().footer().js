// todo tests
// 1- Method returns all <tfoot> elements. Use more than 1 table and attempt to select all footers

describe( "tables- tables().footer()", function() {
	dt.libs( {
		js:  [ 'jquery', 'datatables' ],
		css: [ 'datatables' ]
	} );

	describe("Check the defaults", function () {
		dt.html( 'basic' );
		it("Default should be null", function () {
				});

	});

});

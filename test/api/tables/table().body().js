// todo tests
// 1- Return table try adding a test class to <tbody> then make sure its updated in DOM.
// 2- Make sure returned data is actually a node

describe( "tables- table().body()", function() {
	dt.libs( {
		js:  [ 'jquery', 'datatables' ],
		css: [ 'datatables' ]
	} );

	describe("Check the defaults", function () {
		dt.html( 'basic' );
		it("Default should be null", function () {
				});

	});

});

// todo tests
// 1- Test to make sure each column is actually iterated over
// 2- Test to make sure this.data() == column().data() etc

describe( "columns- columns().every()", function() {
	dt.libs( {
		js:  [ 'jquery', 'datatables' ],
		css: [ 'datatables' ]
	} );

	describe("Check the defaults", function () {
		dt.html( 'basic' );
		it("Default should be null", function () {
				});

	});

});

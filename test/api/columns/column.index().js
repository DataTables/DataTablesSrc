// todo tests
// 1- exactly 2 params and of correct type
// 2- Tests to convert between types on a column
// 3- make sure returned value of calculated colum index is int and is correct


describe( "columns- column.index()", function() {
	dt.libs( {
		js:  [ 'jquery', 'datatables' ],
		css: [ 'datatables' ]
	} );

	describe("Check the defaults", function () {
		dt.html( 'basic' );
		it("Default should be null", function () {
				});

	});

});

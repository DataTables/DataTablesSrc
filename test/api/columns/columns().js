// todo tests
// 1- Able to select all columns
// 2- Only one parameter when selecting all columns and of correct type/ is optional
// 3- Using column-selector there are 2 params and 1 is optional other is not, correct type


describe( "columns- columns()", function() {
	dt.libs( {
		js:  [ 'jquery', 'datatables' ],
		css: [ 'datatables' ]
	} );

	describe("Check the defaults", function () {
		dt.html( 'basic' );
		it("Default should be null", function () {
				});

	});

});

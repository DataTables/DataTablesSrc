// todo tests
// 1- Exactly 2 params and of correct type
// 2- Second param is optional yes/no
// 3- Test to make sure column() has access to full columns API.
// 4- Some tests on the modifier


describe( "columns- column()", function() {
	dt.libs( {
		js:  [ 'jquery', 'datatables' ],
		css: [ 'datatables' ]
	} );

	describe("Check the defaults", function () {
		dt.html( 'basic' );
		it("Default should be null", function () {
				});

	});

});

<?xml version="1.0" encoding="UTF-8" ?>
<dt-example table-type="html-split-name" order="4">

<css lib="datatables" />
<js lib="jquery" />
<js lib="datatables" />

<title>Multi-column sorting</title>
<js><![CDATA[

$(document).ready(function() {
	$('#example').dataTable( {
		columnDefs: [ {
			targets: [ 0 ],
			dataSort: [ 0, 1 ]
		}, {
			targets: [ 1 ],
			dataSort: [ 1, 0 ]
		}, {
			targets: [ 4 ],
			dataSort: [ 4, 0 ]
		} ]
	} );
} );
]]></js>

<info><![CDATA[

DataTables allows sorting by multiple columns at the same time, which can be activated in a number of different ways:

* User shift click on a column (added the clicked column as a secondary, tertiary etc sorting column).
* On a per-column basis (i.e. sort by a specific column and then a secondary column if the data in the first column is identical), through the `dt-init dataSort` option.
* Using the `dt-init sorting` option to specify a multiple column sort by default (for example `[ [0,'asc'], [1,'asc'] ]`).
* Through the `dt-api order()` API method.

Note that, the ability for the user to shift click to sort multiple columns can be disabled through the `dt-init sortMulti` option.

The example below shows the first column having a secondary sort applied to the second column in the table, vice-versa for the second column being tied directly to the first and the salary column to the first name column.

]]></info>

</dt-example>

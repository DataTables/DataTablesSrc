<?xml version="1.0" encoding="UTF-8" ?>
<dt-example table-type="html" order="">

<css lib="datatables" />
<js lib="jquery" />
<js lib="datatables" />

<title>Show / hide columns dynamically</title>
<js><![CDATA[

$(document).ready(function() {
	var table = $('#example').DataTable( {
		"scrollY": "200px",
		"paginate": false
	} );

	$('a.toggle-vis').on( 'click', function (e) {
		e.preventDefault();

		// Get the column API object
		var column = table.column( $(this).attr('data-column') );

		// Toggle the visibility
		column.visible( ! column.visible() );
	} );
} );

]]></js>

<info><![CDATA[

This example shows how you can make use of the `dt-api column().visible()` API method to dynamically show and hide columns in a table. Also included here is scrolling, just to show it enabled with this API method, although that is not required for the API function to work.

In addition to this, groups of columns can be shown and hidden at the same time using the `dt-api columns()` method to select multiple columns and then using the `dt-api columns().visible()` method to set their state.

If you are looking for a more complete column visibility interaction controls (ColVis for DataTables)[http://datatables.net/extras/colvis] provides a complete interface for allowing the user to show and hide columns in the table.

]]></info>

<demo-html>
	<a class="toggle-vis" data-column="0">Toggle column 1<br /></a>
	<a class="toggle-vis" data-column="1">Toggle column 2<br /></a>
	<a class="toggle-vis" data-column="2">Toggle column 3<br /></a>
	<a class="toggle-vis" data-column="3">Toggle column 4<br /></a>
	<a class="toggle-vis" data-column="4">Toggle column 5<br /></a>
</demo-html>

</dt-example>

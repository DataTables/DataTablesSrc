<?xml version="1.0" encoding="UTF-8" ?>
<dt-example table-type="html" order="">

<css lib="bootstrap" />
<css lib="datatables-bootstrap" />
<js lib="jquery" />
<js lib="datatables" />
<js lib="datatables-bootstrap" />

<title>Bootstrap</title>
<js><![CDATA[
$(document).ready(function() {
	$('#example').dataTable();
} );
]]></js>

<info><![CDATA[

DataTables can integrate seamlessly with (Bootstrap)[http://twitter.github.io/bootstrap/] using Bootstrap's (table styling options)[http://twitter.github.io/bootstrap/base-css.html#tables] to present a consistent interface with your Bootstrap driven site / app.

DataTables' Bootstrap integration also provides a renderer for the pagination control in DataTables to ensure that the pagination of the table is also styled consistently by Bootstrap.

This integration is done simply by including the DataTables Bootstrap files (CSS and JS) which will set the defaults needed for DataTables to be initialised as normal, as shown in this examples.

]]></info>

</dt-example>

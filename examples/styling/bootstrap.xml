<?xml version="1.0" encoding="UTF-8" ?>
<dt-example table-type="html" order="9" framework="bootstrap">

<css lib="datatables" />

<title>Bootstrap 3</title>
<js lib="jquery datatables">
<![CDATA[
$(document).ready(function () {
	$('#example').DataTable();
});
]]>
</js>
<js-vanilla>
<![CDATA[
document.addEventListener('DOMContentLoaded', function () {
    new DataTable('#example');
});
]]>
</js-vanilla>

<info><![CDATA[

DataTables can integrate seamlessly with [Bootstrap 3](http://getbootstrap.com/) using Bootstrap's [table styling options](http://getbootstrap.com/css/#tables) to present an interface with a uniform design, based on Bootstrap, for your site / app.

DataTables' Bootstrap integration also provides a renderer for the pagination control in DataTables to ensure that the pagination of the table is also styled consistently by Bootstrap.

This integration is done simply by including the DataTables Bootstrap files (CSS and JS) which sets the defaults needed for DataTables to be initialised as normal, as shown in this example.

]]></info>

</dt-example>

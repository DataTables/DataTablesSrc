<?xml version="1.0" encoding="UTF-8" ?>
<dt-example table-type="html" order="">

<css lib="datatables" />
<js lib="jquery" />
<js lib="datatables" />

<title>DataTables events</title>
<js><![CDATA[

function eventFired( type ) {
	var n = $('demo_info')[0];
	n.innerHTML += '<div>'+type+' event - '+new Date().getTime()+'</div>';
	n.scrollTop = n.scrollHeight;		
}

$(document).ready(function() {
	$('#example')
		.on( 'sort',   function () { eventFired( 'Sort' ); } )
		.on( 'filter', function () { eventFired( 'Filter' ); } )
		.on( 'page',   function () { eventFired( 'Page' ); } )
		.dataTable();
} );

]]></js>

<info><![CDATA[

DataTables fires a number of custom events which you can bind to in the standard jQuery fashion, allowing your code to perform custom actions when these events occur.

This example shows the use of the `sort`, `filter` and `page` events and will add a notification that the event fired to an element on the page to show that they have indeed fired.

]]></info>

<demo-html>
	<div id="demo_info"></div>
</demo-html>

</dt-example>
